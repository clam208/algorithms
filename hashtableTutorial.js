//associative array - when we mess with regular arrays, we normalll have something like
//[3,7,4] and we  access each array element with a numbered index ie arr[0]= 3
//associative arrays allow us to associate data with keys which are nomrall strings
// must learn associative arryas before we can mess with has tables
var assoc =
{

}
assoc.cant = 15
//difference from example above is that any string can now be used as a key
assoc["cant"] =15 
console.log(assoc);


//stores data with a key associated to a piece of data. Most languages already have this build in
//but for the sake of learning lets do this the hard way first
// keys are normally strings
var hashTable = (key,maximum) =>{
var hash = 0;

}